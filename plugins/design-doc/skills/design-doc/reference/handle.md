Name the design-document entry below the way a reader would refer to it across a desk.

Output a phrase of two to five words naming the thing the entry decides, assumes, or asks, never the fact that it was decided. It sits in the middle of a sentence, so it reads as a noun phrase or a short statement.

Rules:

- Two to five words, no trailing punctuation.
- No register ids such as DQ4, A2, Q1, V3, or c-log. When the entry points at another decision, name the thing that decision is about.
- No file paths, and no review findings or passes by number.
- Upper-case acronyms and product names as their owners write them; everything else lower-case.
- Name the thing, not the entry: "24-hour tokens", never "token lifetime decision".

Output only the handle: no quotation marks, no preamble, no notes.

The entry follows as its kind, its title when it has one, and its text.
