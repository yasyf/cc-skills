[packs.general]

# The ccx guard pack blocks the token-heavy read/search primitives so agents reach
# for the cc-context facade (ccx) first. ACTIVATE once cc-context cuts its first
# release: uncomment the three lines below, pin `commit` to that tag's SHA, then run
# `uvx capt-hook pack update`. (capt-hook external packs need a resolvable
# source+commit; an unresolvable placeholder would break every hook run here.)
# [packs.ccx]
# source = "github:yasyf/cc-context"
# commit = "<pinned-sha>"
