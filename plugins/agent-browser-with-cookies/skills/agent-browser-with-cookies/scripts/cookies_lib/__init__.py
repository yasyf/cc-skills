"""Library for agent-browser-with-cookies.

Pure, testable pieces (domains, crypto, serialize) plus the system boundaries
(profiles, keychain, getcookie) the CLI orchestrates. STDLIB + ``cryptography``
only — see scripts/cookies.py for the uv inline-deps header.
"""
