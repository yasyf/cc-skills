name: Plugin autobump

on:
  workflow_call:
    inputs:
      dry-run:
        type: boolean
        default: false
      guard-plugins:
        type: string
        default: ""
      exclude-paths:
        type: string
        default: ""
      strict:
        type: boolean
        default: false

jobs:
  autobump:
    runs-on: ubuntu-latest
    concurrency:
      group: "plugin-autobump-${{ github.repository }}"
      cancel-in-progress: false
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      # job_workflow_sha is unreliable (empty even cross-repo); job_workflow_ref carries repo/path@ref
      - name: Resolve pinned script ref
        id: script-ref
        run: |
          REF="${{ github.job_workflow_sha }}"
          if [ -z "$REF" ] && [ "${{ github.repository }}" = "yasyf/cc-skills" ]; then
            REF="${{ github.sha }}"
          fi
          if [ -z "$REF" ]; then
            JWR="${{ github.job_workflow_ref }}"
            REF="${JWR#*@}"
          fi
          if [ -z "$REF" ]; then
            echo "::error::cannot resolve a pinned script ref — refusing an unpinned checkout"
            exit 1
          fi
          echo "resolved script ref: $REF (job_workflow_sha='${{ github.job_workflow_sha }}' job_workflow_ref='${{ github.job_workflow_ref }}')"
          echo "ref=$REF" >> "$GITHUB_OUTPUT"

      # Script + workflow stay in lockstep at the caller's pinned ref — never main.
      - uses: actions/checkout@v7
        with:
          repository: yasyf/cc-skills
          ref: ${{ steps.script-ref.outputs.ref }}
          path: .autobump-src
          sparse-checkout: .github/scripts

      - uses: astral-sh/setup-uv@v8.2.0

      - name: Detect drift and bump
        env:
          AUTOBUMP_DRY_RUN: ${{ inputs.dry-run }}
          AUTOBUMP_GUARD: ${{ inputs.guard-plugins }}
          AUTOBUMP_EXCLUDES: ${{ inputs.exclude-paths }}
          AUTOBUMP_STRICT: ${{ github.event_name == 'schedule' || inputs.strict }}
        run: uv run .autobump-src/.github/scripts/plugin_autobump.py
