{"v":1,"lamport":4,"session":"053e6c67-586d-4121-ba6e-50f636dbdf07","ops":[{"kind":"set_criterion_status","id":"e40fce242b5db37c84d68fe1e8f27ec2","status":"met","note":"Done in `baffb08`. The deferral comment on `gtProbeTimeout` — which named `WaitDelay` as the fix and explained why it was being deferred — is gone, replaced by the measurement provenance that the constant actually needs: 60 runs, min 5.47s, median ~7.0s, max 13.06s, plus a separate 17.9s reading, and an explicit note that the old 5s value was derived from a single ~1.5s sample. The falsified \"~1.5s, so this is roughly 3x headroom\" claim is deleted.\n\nThe paragraph describing how a grandchild could outlast the deadline is also gone from that comment, because it is no longer a known limitation — `RunCLIProbeDir` fixes it, and the behavior is now asserted by `TestRunCLIProbeDirKillsDescendants` rather than documented as outstanding."}]}