{"v":1,"lamport":1,"session":"d30071c4-4607-47a2-abf5-149cd7f2d40f","ops":[{"kind":"create_note","nonce":"d0d298ae43ff3e4bedf10f16c1211fa7","title":"slop-cop check flag is --lang (not --markdown) since v0.1.14; 'unknown flag: --lang' = a stale binary, not a docs bug; two installed binaries + installer fast-path gotcha","body":"The `slop-cop check` language flag is **`--lang`** (`auto|text|markdown|html|jsx|tsx|ts|js`),\nshipped in release **v0.1.14**. It replaced the old `--markdown` (`auto|on|off`) flag in commit\n`90f9f44`, which landed *after* tag `v0.1.10`. So `slop-cop: unknown flag: --lang` means a\n**stale binary**, not a docs/skill bug — docs/skills (cc-factory AGENTS.md, slop-cop/writing-docs/\nrepo-bootstrap skills) correctly use `--lang=markdown`.\n\nTwo slop-cop binaries exist on this machine:\n- **Live**: `~/.claude/plugins/data/slop-cop-skills/bin/slop-cop` — what the skill uses; refreshed\n  by `install-binary.sh` (downloads `/releases/latest`). Currently v0.1.14, has `--lang`.\n- **Stale fallback**: `~/.claude/plugins/cache/slop-cop/slop-cop/0.1.0/bin/slop-cop` — v0.1.10\n  (commit `2355d4c`), only knows `--markdown`. Bundled in the cached plugin (manifest 0.1.0);\n  the skill's tier-3 fallback (used only if the live binary is missing). **Not on PATH.**\n\nGotchas:\n- `install-binary.sh` **fast-paths out without downloading** if any working binary already exists\n  (`$BIN_PATH version` exits 0). To force an upgrade: `rm` the target binary first, then run with\n  `CLAUDE_PLUGIN_ROOT=\u003cdir-whose-/bin-holds-the-binary\u003e` (e.g. the `plugins/data/slop-cop-skills`\n  dir) so it writes back to the same place.\n- The rtk Bash hook (`rtk hook claude`) does **not** rewrite `slop-cop` (only known commands like\n  `git`), and slop-cop isn't on PATH — so a bare `slop-cop ...` in the Bash-tool env is\n  `command not found`, not a slop-cop error.\n\nSee [[slop-cop-repo]] for source location, build, and the auto-release model.\n\n**Inversion caveat (2026-07-22 audit, adversarially verified):** in the recent transcript corpus, \"unknown flag\" errors were dominated by *hallucinated or wrong-subcommand flags against CURRENT binaries* (e.g. `ccx --budget` on a subcommand that takes it elsewhere, `task add --title`), with ZERO stale-binary cases in an 8-sample read-back. Check `\u003ctool\u003e --version` / `--help` before assuming staleness — the stale-binary explanation above is the exception now, not the default.","tags":["memory","memory:slop-cop-binary-resolution","memory-type:reference"],"anchors":[]}]}