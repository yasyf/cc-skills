{"v":1,"lamport":1,"session":"e70c7f32-e3ee-4e88-9845-b3fd2ac15304","ops":[{"kind":"create_note","nonce":"ef817b1a903c52387b845c32cf96f8b6","title":"Docs builds: hard 5-min cap + grouped-reference collapse (gd-build)","body":"Shipped in cc-skills 2fc32523da63. Two central changes to the fleet docs pipeline (tools/gd-build):\n\n1. **Hard cap**: `gd-build build` supervises the real build (`python -m gd_build _build`) in its own process group; default 300s wall-clock, `GD_BUILD_TIMEOUT=\u003csecs\u003e` overrides, `0` disables; exit 124 with the quarto/pandoc tree killed on expiry; external SIGTERM/SIGINT forwarded (exit 128+n) so a cancelled supervisor never orphans a runaway build. Effective fleet-wide immediately: CI pulls gd-build from cc-skills@main at build time.\n\n2. **grouped-reference-pages patch** (third version-gated patch in gd_build/patches.py, logic in gd_build/grouped_reference.py): collapses a curated `reference:` into one anchored page per titled group, disables great-docs' \u003e5-method class splitting, pins every heading (members too) to `id=doc.anchor`, re-points summary/sidebar/inventory links, sidebar membership via fragment-free group leaves. captain-hook: ~296 reference HTML pages → 13; CI build projected ~9min → ~3min. Kill switch `GD_BUILD_PATCHES=none`; any probe mismatch degrades to stock loudly (UNPATCHED warning).\n\nDiagnosis of record: the 84-min local build was bare `great-docs build` in a fresh worktree (gitignored titles script absent → pandoc #11687 backtracking), compounded by the workstation's per-exec code-signing serialization under load (~2.2x slower than CI; load avg \u003e100 makes local wall-clock meaningless — judge builds by per-stage CI projection).\n\nKnown follow-ups: (a) capt-hook's build logs \"Dynamic introspection failed for a resolved item, retrying with static analysis\" — the ENTIRE reference silently re-analyzes statically (~20-90s waste) and 5 config-listed symbols (COMMENT_TYPES, HookResponse, InlineTests, TCondition, ToolCall) are excluded as cyclic aliases; worth a capt-hook-side investigation. (b) `fast_sidebar: true` in capt-hook's great-docs.yml is a no-op key in great-docs 0.15 (see papercut). (c) Old per-symbol reference URLs (reference/\u003cSymbol\u003e.html) 404 after the collapse; interlinks/inventory regenerate correctly.","tags":null,"anchors":[{"kind":"path","value":"tools/gd-build"}]}]}