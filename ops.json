{"v":1,"lamport":1,"ops":[{"kind":"create_note","nonce":"c369d9a51cdf776ae6ba96ea30764379","title":"Harness bug: context:fork skill executions inherit the calling subagent's StructuredOutput tool; a fork ending its turn on it returns a bare 'Skill execution completed' stub and the answer is lost — applies to ANY forked skill invoked from a schema-bound","body":"Claude Code's forked skill execution (`context: fork` frontmatter) does not isolate the caller's tool inventory: a workflow `agent(..., {schema})` wrapper's `StructuredOutput` tool is visible inside the fork, and that tool sets `toolEndsTurn: true`. A fork that \"returns\" via it ends with no trailing assistant text; the Skill relay harvests only trailing text, so the caller receives the literal 78-char stub `Skill \"codex\" completed (forked execution). Result: Skill execution completed` and the fork's real answer is discarded.\n\nDiagnosed 2026-07-08 from cc-context session `b09066d0` (workflow `wf_e60273d5-c47`): 18/18 forks spawned fine and ran 1–6 minutes; all 15 stubs = fork ended on the leaked tool; all 3 successes = fork ended with plain text (2 via ReportFindings, which doesn't end the turn). Not load, rate limits, or auth. Applies to ANY `context: fork` skill invoked from a schema-bound subagent, not just codex. Symptom to recognize: a minutes-long Skill call returning the bare stub.\n\nFix pattern: don't nest Skill forks inside subagents — ship the recipe as a plugin agent (codex plugin 0.9.0 `codex:codex-wrapper`, see [[model-routing-convention]]). Upstream bug unfiled as of 2026-07-08.","tags":["memory","memory:codex-fork-structured-output-leak","memory-type:reference"],"anchors":[]}]}